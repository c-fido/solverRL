[pytest]
pythonpath = .
testpaths = tests
# PR / default local: skip long DoorKey transfer. Override with: pytest -m ""
addopts = -m "not slow"
markers =
    slow: long-running (DoorKey full transfer); excluded by default
    integration: short PPO/SB3 trains, census, or EXPAND smokes (CI still runs these)
